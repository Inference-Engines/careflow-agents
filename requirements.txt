# CareFlow Agents — Python dependencies
# CareFlow 에이전트 Python 의존성

# Google ADK (Agent Development Kit)
google-adk>=0.2.0

# MCP Toolbox client — connects agent to the Toolbox Cloud Run service
# careflow/db/mcp_client.py: from toolbox_core import ToolboxClient
toolbox-core>=0.1.0

# AlloyDB / PostgreSQL connectivity (AlloyDB first, mock fallback)
# AlloyDB / PostgreSQL 연결용 — 미설치 시 mock 데이터로 자동 fallback
sqlalchemy>=2.0.0
pg8000>=1.30.0

# .env loading — careflow/config.py uses load_dotenv()
python-dotenv>=1.0.0

# UI Proxy Server (ui/server.py)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
python-multipart>=0.0.9
